// Components
import { AetherView, AetherText } from 'aetherspace/primitives';

/* --- <DashboardScreen/> ---------------------------------------------------------------------- */

const DashboardScreen = (props) => {

    // -- Render --

    return (
        <AetherView>
            <AetherText>Hello Admin</AetherText>
        </AetherView>
    );
};

/* --- Exports --------------------------------------------------------------------------------- */

export default DashboardScreen;
